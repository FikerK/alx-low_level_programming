#include "main.h"

/**
 * add - adds two integers
 *
 * @i: int to be added
 * @j: int to be added
 * Return: 0
 */
int add(int i, int j)
{
	int k = 0;
	/*int a = 0, k = 0, l = 0, m = 0, n = 0, o = 0, p = 0, q = 0;*/

	k = i + j;
	/**
	* a = k / 10;
	* while (a >= 10)
	* {
	* 	p++;
	* 	a = a / 10;
	* }
	* for (m = p; m >= 0; m--)
	*{
	* 	q = m;
	*	while (q > 0)
	* 	{
	*		n = 10 * n;
	* 	q--;
	*	}
	*	l = k / n;
	*	_putchar(l + '0');
	* }
	* if (k <= 10)
	* {
	*	_putchar(k + '0');
	* }
	* else
	* {
	*	o = k % 10;
	*	_putchar(o + '0');
	* }
	*/
	return (k);
}
